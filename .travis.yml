language: generic
sudo: required
services:
 - docker

before_install:
 - docker build -t adamagatt/docker-react -f Dockerfile.dev .

script:
 - docker run -e CI=true adamagatt/docker-react npm run test